// Optional runtime override for the platform API base URL.
// Leave empty for local development so the Angular app falls back to localhost.
window.__WUSELVERSE_API_URL__ = window.__WUSELVERSE_API_URL__ || '';
